/* Quest script disabled — job advancement handled by instructor NPCs directly */
var status = -1;
function start(mode, type, selection) { qm.dispose(); }
function end(mode, type, selection) { qm.dispose(); }
